# Copy to .env (git-ignored) and fill in with `terraform output` values.
# Credentials themselves come from the ~/.aws profile, never from this file.

DATA_BUCKET=jjs-project-3-de-portfolio-<account_id>
AWS_REGION=eu-west-2
